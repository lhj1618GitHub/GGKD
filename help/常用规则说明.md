# [官方说明文档](https://gkd.li/api/) - [快照审查工具](https://i.gkd.li/) - [增强脚本](https://github.com/adproqwq/MikuMagicWand/releases)

- ## [在线json5格式化查错工具](https://lhj1618github.github.io/html/json-json5-tool.html) - [离线含json5库版本](https://lhj1618github.github.io/html/json5-tool/json-json5.html)

***

# 全局规则
```txt
{
    key: 0,
    name: '开屏广告',
    desc: '跳过/关闭',
    fastQuery: true,
    actionMaximum: 1,
    matchTime: 5000,
    resetMatch: 'app',
    anyMatches: [
        '[text*="跳过" || text*="关闭"][text.length<10][visibleToUser=true]',
    ],
}
```

# 订阅文件格式
```txt
{
  id: 订阅文件的标识(数字-新旧保持一致，否则更新失败),
  name: '订阅的名称',
  version: 订阅版本号(大的替换小的),
  author: '作者名称',
  updateUrl: 'json5更新下载链接，检查版本号',
  checkUpdateUrl: 'json5更新检查链接,检查标识和版本',
  supportUri: '自定义链接,点击[用户反馈]时, 打开此链接(https://gkd.li/)',
  categories: [此订阅的应用规则分类列表],
  globalGroups: [此订阅的全局规则组列表],
  apps: [所有应用规则列表],
}
```

# 点击位置坐标偏移-点击相对坐标：
```json
{
  position: {
    left: 'width * 0.4981',
    top: 'width * 1.6250',
  },
}
```

# 强制点击坐标
```json
{
    matches: [
        "[text*='广告']",
    ],
    action: 'clickCenter',
    position: {
        x: 0.5,
        y: 0.1,
    },
}
```

# 常用参数

- `action: 'back',`：使用返回键
- `action: 'click',`： 默认点击

   -为默认值, 如果目标节点是 clickable 的, 则使用 `clickNode`, 反之使用 `clickCenter`;并且当 `clickNode` 事件没有被应用接收时, 则使用 `clickCenter`;
  
- `action: 'longClick',`：长按

   -长按时间为 500 毫秒；如果目标节点是 longClickable 的, 则使用 `longClickNode`, 反之使用 `longClickCenter`；并且当 `longClickNode` 事件没有被应用接收时, 则使用 `longClickCenter`；
  
- `action: 'none',`：什么都不做；仅作为匹配标记使用;

***

# END
